import React, { Component } from 'react';

import ProcessManager from './processManager';

const HomePage = () => {
  return (
    <div className="container-router window">
        <ProcessManager />
    </div>
  );
};

export default HomePage;
